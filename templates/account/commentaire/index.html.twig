{% extends 'account/base_account.html.twig' %}

{% block title %}Commentaire index{% endblock %}

{% block body %}
    <h1>Commentaires</h1>

    <table class="table">
        <thead>
        <tr>
            <th>validé</th>
            <th>Contenu</th>
            <th>Date</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for commentaire in commentaires %}
            <tr>
                <td>{{ commentaire.isValide == true ? 'oui' : 'non' }}</td>
                <td>{{ commentaire.contenu }}</td>
                <td>{{ commentaire.date ? commentaire.date|date('d-m-Y') : '' }}</td>
                <td>
                    <a href="{{ path('account_commentaire_show', {'id': commentaire.id}) }}">show</a>
                    {{ include('account/commentaire/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
