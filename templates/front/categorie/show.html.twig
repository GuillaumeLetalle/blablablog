{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>
<div>
    <a href="{{ path('front_categorie_index') }}">Retour à la liste des catégories</a>
</div>
<div>
<a href="{{ path('front_article_index', {'idCategorie': categorie.id}) }}">Les articles liés à cette categorie</a>
</div>
{% endblock %}
