{% extends 'admin/base_admin.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>
<div>
    <a href="{{ path('app_categorie_index') }}">back to list</a>
</div>
<div>
<a href="{{ path('app_article_index', {'idCategorie': categorie.id}) }}">Show articles categorie</a>
</div>
<div>
    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>
</div>
<div>
    {{ include('admin/categorie/_delete_form.html.twig') }}
</div>
{% endblock %}
